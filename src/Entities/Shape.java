package Entities;

// Interface Shape define um contrato para classes que implementam ela.
public interface Shape {

    // Método abstrato area que deve ser implementado pelas classes.
    // Retorna a área da forma como um valor double.
    double area();
}